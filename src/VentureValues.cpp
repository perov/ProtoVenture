
#include "Header.h"

// We do not need it?
/*
template< typename T >
class TypedProperty : public Property
{
public:
    TypedProperty (const T& data)
      : data(data) {};
private:
    T data;
};
*/
